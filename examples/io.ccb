effect io with
| print(x : Text) : Unit
| read() : Text 
| printNumber(x : Int) : Unit

let safe square(safe x) = x * x

let safe<ref,exit> squarePrint(safe<ref,exit> x) =
    let y = x * x in printNumber(y) ; y

let safe shouldFail = print("foo")
